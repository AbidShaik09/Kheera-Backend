services:

  kheera-backend-dev:
    build: .

    container_name: kheera-backend-dev

    restart: unless-stopped

    env_file:
      - .env.dev

    ports:
      - "8081:8080"

    networks:
      - app-network

networks:
  app-network:
    external: true
